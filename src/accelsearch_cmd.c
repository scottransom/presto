/****
  command line parser -- generated by pyclig
  (https://github.com/scottransom/pyCLIG)

  Based on the original clig by Harald Kirsch:
  (C) 1995-2004 Harald Kirsch (clig@geggus.net)
  pyCLIG (C) 2026 Scott Ransom and Claude (Anthropic)
****/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <errno.h>
#include <limits.h>
#include <float.h>
#include <math.h>

#include "accelsearch_cmd.h"

char *Program;

static Cmdline cmd = {
    /***** -ncpus: Number of processors to use with OpenMP */
    /* ncpusP = */ 1,
    /* ncpus = */ 1,
    /* ncpusC = */ 1,
    /***** -lobin: The first Fourier frequency in the data file */
    /* lobinP = */ 1,
    /* lobin = */ 0,
    /* lobinC = */ 1,
    /***** -numharm: The number of harmonics to sum (power-of-two) */
    /* numharmP = */ 1,
    /* numharm = */ 8,
    /* numharmC = */ 1,
    /***** -zmax: The max (+ and -) Fourier freq deriv to search */
    /* zmaxP = */ 1,
    /* zmax = */ 200,
    /* zmaxC = */ 1,
    /***** -wmax: The max (+ and -) Fourier freq double derivs to search */
    /* wmaxP = */ 0,
    /* wmax = */ 0,
    /* wmaxC = */ 0,
    /***** -sigma: Cutoff sigma for choosing candidates */
    /* sigmaP = */ 1,
    /* sigma = */ 2.0,
    /* sigmaC = */ 1,
    /***** -rlo: The lowest Fourier frequency (of the highest harmonic!) to search */
    /* rloP = */ 0,
    /* rlo = */ 0,
    /* rloC = */ 0,
    /***** -rhi: The highest Fourier frequency (of the highest harmonic!) to search */
    /* rhiP = */ 0,
    /* rhi = */ 0,
    /* rhiC = */ 0,
    /***** -flo: The lowest frequency (Hz) (of the highest harmonic!) to search */
    /* floP = */ 1,
    /* flo = */ 1.0,
    /* floC = */ 1,
    /***** -fhi: The highest frequency (Hz) (of the highest harmonic!) to search */
    /* fhiP = */ 1,
    /* fhi = */ 10000.0,
    /* fhiC = */ 1,
    /***** -inmem: Compute full f-fdot plane in memory.  Very fast, but only for short time series. */
    /* inmemP = */ 0,
    /***** -photon: Data is poissonian so use freq 0 as power normalization */
    /* photonP = */ 0,
    /***** -median: Use block-median power normalization (default) */
    /* medianP = */ 0,
    /***** -locpow: Use double-tophat local-power normalization (not usually recommended) */
    /* locpowP = */ 0,
    /***** -zaplist: A file of freqs+widths to zap from the FFT (only if the input file is a *.[s]dat file) */
    /* zaplistP = */ 0,
    /* zaplist = */ NULL,
    /* zaplistC = */ 0,
    /***** -baryv: The radial velocity component (v/c) towards the target during the obs */
    /* baryvP = */ 1,
    /* baryv = */ 0.0,
    /* baryvC = */ 1,
    /***** -otheropt: Use the alternative optimization (for testing/debugging) */
    /* otheroptP = */ 0,
    /***** -noharmpolish: Do not use 'harmpolish' by default */
    /* noharmpolishP = */ 0,
    /***** -noharmremove: Do not remove harmonically related candidates (never removed for numharm = 1) */
    /* noharmremoveP = */ 0,
    /***** uninterpreted rest of command line */
    /* argc = */ 0,
    /* argv = */ NULL,
    /***** the original command line concatenated */
    /* full_cmd_line = */ NULL
};

/*********************************************************************/

static void checkFloatConversion(double v, const char *option, const char *arg)
{
    const char *err = NULL;

    if ((errno == ERANGE && v != 0.0)
        || (v < HUGE_VAL && v > -HUGE_VAL && (v < 0.0 ? -v : v) > (double) FLT_MAX)) {
        err = "large";
    } else if ((errno == ERANGE && v == 0.0)
               || (v != 0.0 && (v < 0.0 ? -v : v) < (double) FLT_MIN)) {
        err = "small";
    }
    if (err) {
        fprintf(stderr,
                "%s: parameter `%s' of option `%s' too %s to represent\n",
                Program, arg, option, err);
        exit(EXIT_FAILURE);
    }
}

/*********************************************************************/

int getIntOpt(int argc, char **argv, int i, int *value, int force)
{
    char *end;
    long v;

    if (++i >= argc)
        goto nothingFound;

    errno = 0;
    v = strtol(argv[i], &end, 0);

    if (end == argv[i])
        goto nothingFound;

    while (isspace((unsigned char) *end))
        end++;
    if (*end)
        goto nothingFound;

    if (errno == ERANGE || v > (long) INT_MAX || v < (long) INT_MIN) {
        fprintf(stderr,
                "%s: parameter `%s' of option `%s' too large to represent\n",
                Program, argv[i], argv[i - 1]);
        exit(EXIT_FAILURE);
    }
    *value = (int) v;
    return i;

  nothingFound:
    if (!force)
        return i - 1;
    fprintf(stderr,
            "%s: missing or malformed integer value after option `%s'\n",
            Program, argv[i - 1]);
    exit(EXIT_FAILURE);
}

/*********************************************************************/

int getFloatOpt(int argc, char **argv, int i, float *value, int force)
{
    char *end;
    double v;

    if (++i >= argc)
        goto nothingFound;

    errno = 0;
    v = strtod(argv[i], &end);

    if (end == argv[i])
        goto nothingFound;

    while (isspace((unsigned char) *end))
        end++;
    if (*end)
        goto nothingFound;

    checkFloatConversion(v, argv[i - 1], argv[i]);
    *value = (float) v;
    return i;

  nothingFound:
    if (!force)
        return i - 1;
    fprintf(stderr,
            "%s: missing or malformed float value after option `%s'\n",
            Program, argv[i - 1]);
    exit(EXIT_FAILURE);
}

/*********************************************************************/

int getDoubleOpt(int argc, char **argv, int i, double *value, int force)
{
    char *end;

    if (++i >= argc)
        goto nothingFound;

    errno = 0;
    *value = strtod(argv[i], &end);

    if (end == argv[i])
        goto nothingFound;

    while (isspace((unsigned char) *end))
        end++;
    if (*end)
        goto nothingFound;

    if (errno == ERANGE) {
        fprintf(stderr,
                "%s: parameter `%s' of option `%s' too %s to represent\n",
                Program, argv[i], argv[i - 1], (*value == 0.0 ? "small" : "large"));
        exit(EXIT_FAILURE);
    }
    return i;

  nothingFound:
    if (!force)
        return i - 1;
    fprintf(stderr,
            "%s: missing or malformed value after option `%s'\n",
            Program, argv[i - 1]);
    exit(EXIT_FAILURE);
}

/*********************************************************************/

int getStringOpt(int argc, char **argv, int i, char **value, int force)
{
    i += 1;
    if (i >= argc) {
        if (force) {
            fprintf(stderr, "%s: missing string after option `%s'\n",
                    Program, argv[i - 1]);
            exit(EXIT_FAILURE);
        }
        return i - 1;
    }

    if (!force && argv[i][0] == '-')
        return i - 1;
    *value = argv[i];
    return i;
}

/*********************************************************************/

void checkIntLower(const char *opt, int *values, int count, int max)
{
    int i;
    for (i = 0; i < count; i++) {
        if (values[i] <= max)
            continue;
        fprintf(stderr,
                "%s: parameter %d of option `%s' greater than max=%d\n",
                Program, i + 1, opt, max);
        exit(EXIT_FAILURE);
    }
}

/*********************************************************************/

void checkIntHigher(const char *opt, int *values, int count, int min)
{
    int i;
    for (i = 0; i < count; i++) {
        if (values[i] >= min)
            continue;
        fprintf(stderr,
                "%s: parameter %d of option `%s' smaller than min=%d\n",
                Program, i + 1, opt, min);
        exit(EXIT_FAILURE);
    }
}

/*********************************************************************/

void checkFloatLower(const char *opt, float *values, int count, float max)
{
    int i;
    for (i = 0; i < count; i++) {
        if (values[i] <= max)
            continue;
        fprintf(stderr,
                "%s: parameter %d of option `%s' greater than max=%f\n",
                Program, i + 1, opt, (double) max);
        exit(EXIT_FAILURE);
    }
}

/*********************************************************************/

void checkFloatHigher(const char *opt, float *values, int count, float min)
{
    int i;
    for (i = 0; i < count; i++) {
        if (values[i] >= min)
            continue;
        fprintf(stderr,
                "%s: parameter %d of option `%s' smaller than min=%f\n",
                Program, i + 1, opt, (double) min);
        exit(EXIT_FAILURE);
    }
}

/*********************************************************************/

void checkDoubleLower(const char *opt, double *values, int count, double max)
{
    int i;
    for (i = 0; i < count; i++) {
        if (values[i] <= max)
            continue;
        fprintf(stderr,
                "%s: parameter %d of option `%s' greater than max=%f\n",
                Program, i + 1, opt, max);
        exit(EXIT_FAILURE);
    }
}

/*********************************************************************/

void checkDoubleHigher(const char *opt, double *values, int count, double min)
{
    int i;
    for (i = 0; i < count; i++) {
        if (values[i] >= min)
            continue;
        fprintf(stderr,
                "%s: parameter %d of option `%s' smaller than min=%f\n",
                Program, i + 1, opt, min);
        exit(EXIT_FAILURE);
    }
}

/*********************************************************************/

static char *catArgv(int argc, char **argv)
{
    int i;
    size_t l, pos, n;
    char *s;

    for (i = 0, l = 1; i < argc; i++)
        l += strlen(argv[i]) + 1;
    s = (char *) malloc(l);
    if (!s) {
        fprintf(stderr, "%s: out of memory\n", Program);
        exit(EXIT_FAILURE);
    }
    for (i = 0, pos = 0; i < argc; i++) {
        n = strlen(argv[i]);
        if (i > 0)
            s[pos++] = ' ';
        memcpy(s + pos, argv[i], n);
        pos += n;
    }
    s[pos] = '\0';
    return s;
}

/*********************************************************************/

void showOptionValues(void)
{
    int i;

    printf("Full command line is:\n`%s'\n", cmd.full_cmd_line);

    /***** -ncpus: Number of processors to use with OpenMP */
    if (!cmd.ncpusP) {
        printf("-ncpus not found.\n");
    } else {
        printf("-ncpus found:\n");
        if (!cmd.ncpusC) {
            printf("  no values\n");
        } else {
            printf("  value = `%d'\n", cmd.ncpus);
        }
    }

    /***** -lobin: The first Fourier frequency in the data file */
    if (!cmd.lobinP) {
        printf("-lobin not found.\n");
    } else {
        printf("-lobin found:\n");
        if (!cmd.lobinC) {
            printf("  no values\n");
        } else {
            printf("  value = `%d'\n", cmd.lobin);
        }
    }

    /***** -numharm: The number of harmonics to sum (power-of-two) */
    if (!cmd.numharmP) {
        printf("-numharm not found.\n");
    } else {
        printf("-numharm found:\n");
        if (!cmd.numharmC) {
            printf("  no values\n");
        } else {
            printf("  value = `%d'\n", cmd.numharm);
        }
    }

    /***** -zmax: The max (+ and -) Fourier freq deriv to search */
    if (!cmd.zmaxP) {
        printf("-zmax not found.\n");
    } else {
        printf("-zmax found:\n");
        if (!cmd.zmaxC) {
            printf("  no values\n");
        } else {
            printf("  value = `%d'\n", cmd.zmax);
        }
    }

    /***** -wmax: The max (+ and -) Fourier freq double derivs to search */
    if (!cmd.wmaxP) {
        printf("-wmax not found.\n");
    } else {
        printf("-wmax found:\n");
        if (!cmd.wmaxC) {
            printf("  no values\n");
        } else {
            printf("  value = `%d'\n", cmd.wmax);
        }
    }

    /***** -sigma: Cutoff sigma for choosing candidates */
    if (!cmd.sigmaP) {
        printf("-sigma not found.\n");
    } else {
        printf("-sigma found:\n");
        if (!cmd.sigmaC) {
            printf("  no values\n");
        } else {
            printf("  value = `%.40g'\n", cmd.sigma);
        }
    }

    /***** -rlo: The lowest Fourier frequency (of the highest harmonic!) to search */
    if (!cmd.rloP) {
        printf("-rlo not found.\n");
    } else {
        printf("-rlo found:\n");
        if (!cmd.rloC) {
            printf("  no values\n");
        } else {
            printf("  value = `%.40g'\n", cmd.rlo);
        }
    }

    /***** -rhi: The highest Fourier frequency (of the highest harmonic!) to search */
    if (!cmd.rhiP) {
        printf("-rhi not found.\n");
    } else {
        printf("-rhi found:\n");
        if (!cmd.rhiC) {
            printf("  no values\n");
        } else {
            printf("  value = `%.40g'\n", cmd.rhi);
        }
    }

    /***** -flo: The lowest frequency (Hz) (of the highest harmonic!) to search */
    if (!cmd.floP) {
        printf("-flo not found.\n");
    } else {
        printf("-flo found:\n");
        if (!cmd.floC) {
            printf("  no values\n");
        } else {
            printf("  value = `%.40g'\n", cmd.flo);
        }
    }

    /***** -fhi: The highest frequency (Hz) (of the highest harmonic!) to search */
    if (!cmd.fhiP) {
        printf("-fhi not found.\n");
    } else {
        printf("-fhi found:\n");
        if (!cmd.fhiC) {
            printf("  no values\n");
        } else {
            printf("  value = `%.40g'\n", cmd.fhi);
        }
    }

    /***** -inmem: Compute full f-fdot plane in memory.  Very fast, but only for short time series. */
    if (!cmd.inmemP) {
        printf("-inmem not found.\n");
    } else {
        printf("-inmem found:\n");
    }

    /***** -photon: Data is poissonian so use freq 0 as power normalization */
    if (!cmd.photonP) {
        printf("-photon not found.\n");
    } else {
        printf("-photon found:\n");
    }

    /***** -median: Use block-median power normalization (default) */
    if (!cmd.medianP) {
        printf("-median not found.\n");
    } else {
        printf("-median found:\n");
    }

    /***** -locpow: Use double-tophat local-power normalization (not usually recommended) */
    if (!cmd.locpowP) {
        printf("-locpow not found.\n");
    } else {
        printf("-locpow found:\n");
    }

    /***** -zaplist: A file of freqs+widths to zap from the FFT (only if the input file is a *.[s]dat file) */
    if (!cmd.zaplistP) {
        printf("-zaplist not found.\n");
    } else {
        printf("-zaplist found:\n");
        if (!cmd.zaplistC) {
            printf("  no values\n");
        } else {
            printf("  value = `%s'\n", cmd.zaplist);
        }
    }

    /***** -baryv: The radial velocity component (v/c) towards the target during the obs */
    if (!cmd.baryvP) {
        printf("-baryv not found.\n");
    } else {
        printf("-baryv found:\n");
        if (!cmd.baryvC) {
            printf("  no values\n");
        } else {
            printf("  value = `%.40g'\n", cmd.baryv);
        }
    }

    /***** -otheropt: Use the alternative optimization (for testing/debugging) */
    if (!cmd.otheroptP) {
        printf("-otheropt not found.\n");
    } else {
        printf("-otheropt found:\n");
    }

    /***** -noharmpolish: Do not use 'harmpolish' by default */
    if (!cmd.noharmpolishP) {
        printf("-noharmpolish not found.\n");
    } else {
        printf("-noharmpolish found:\n");
    }

    /***** -noharmremove: Do not remove harmonically related candidates (never removed for numharm = 1) */
    if (!cmd.noharmremoveP) {
        printf("-noharmremove not found.\n");
    } else {
        printf("-noharmremove found:\n");
    }

    if (!cmd.argc) {
        printf("no remaining parameters in argv\n");
    } else {
        printf("argv =");
        for (i = 0; i < cmd.argc; i++)
            printf(" `%s'", cmd.argv[i]);
        printf("\n");
    }
}

/*********************************************************************/

void usage(void)
{
    fputs("   [-ncpus ncpus] [-lobin lobin] [-numharm numharm] [-zmax zmax] [-wmax wmax] [-sigma sigma] [-rlo rlo] [-rhi rhi] [-flo flo] [-fhi fhi] [-inmem] [-photon] [-median] [-locpow] [-zaplist zaplist] [-baryv baryv] [-otheropt] [-noharmpolish] [-noharmremove] [--] infile ...\n", stderr);
    fputs("      Search an FFT or short time series for pulsars using a Fourier domain acceleration search with harmonic summing.\n", stderr);
    fputs("           -ncpus: Number of processors to use with OpenMP\n", stderr);
    fputs("                   1 int value between 1 and oo\n", stderr);
    fputs("                   default: `1'\n", stderr);
    fputs("           -lobin: The first Fourier frequency in the data file\n", stderr);
    fputs("                   1 int value between 0 and oo\n", stderr);
    fputs("                   default: `0'\n", stderr);
    fputs("         -numharm: The number of harmonics to sum (power-of-two)\n", stderr);
    fputs("                   1 int value between 1 and 32\n", stderr);
    fputs("                   default: `8'\n", stderr);
    fputs("            -zmax: The max (+ and -) Fourier freq deriv to search\n", stderr);
    fputs("                   1 int value between 0 and 1200\n", stderr);
    fputs("                   default: `200'\n", stderr);
    fputs("            -wmax: The max (+ and -) Fourier freq double derivs to search\n", stderr);
    fputs("                   1 int value between 0 and 4000\n", stderr);
    fputs("           -sigma: Cutoff sigma for choosing candidates\n", stderr);
    fputs("                   1 float value between 1.0 and 30.0\n", stderr);
    fputs("                   default: `2.0'\n", stderr);
    fputs("             -rlo: The lowest Fourier frequency (of the highest harmonic!) to search\n", stderr);
    fputs("                   1 double value between 0.0 and oo\n", stderr);
    fputs("             -rhi: The highest Fourier frequency (of the highest harmonic!) to search\n", stderr);
    fputs("                   1 double value between 0.0 and oo\n", stderr);
    fputs("             -flo: The lowest frequency (Hz) (of the highest harmonic!) to search\n", stderr);
    fputs("                   1 double value between 0.0 and oo\n", stderr);
    fputs("                   default: `1.0'\n", stderr);
    fputs("             -fhi: The highest frequency (Hz) (of the highest harmonic!) to search\n", stderr);
    fputs("                   1 double value between 0.0 and oo\n", stderr);
    fputs("                   default: `10000.0'\n", stderr);
    fputs("           -inmem: Compute full f-fdot plane in memory.  Very fast, but only for short time series.\n", stderr);
    fputs("          -photon: Data is poissonian so use freq 0 as power normalization\n", stderr);
    fputs("          -median: Use block-median power normalization (default)\n", stderr);
    fputs("          -locpow: Use double-tophat local-power normalization (not usually recommended)\n", stderr);
    fputs("         -zaplist: A file of freqs+widths to zap from the FFT (only if the input file is a *.[s]dat file)\n", stderr);
    fputs("                   1 char* value\n", stderr);
    fputs("           -baryv: The radial velocity component (v/c) towards the target during the obs\n", stderr);
    fputs("                   1 double value between -0.1 and 0.1\n", stderr);
    fputs("                   default: `0.0'\n", stderr);
    fputs("        -otheropt: Use the alternative optimization (for testing/debugging)\n", stderr);
    fputs("    -noharmpolish: Do not use 'harmpolish' by default\n", stderr);
    fputs("    -noharmremove: Do not remove harmonically related candidates (never removed for numharm = 1)\n", stderr);
    fputs("           infile: Input file name(s) of the floating point .fft or .[s]dat file(s).  '.inf' file(s) of the same name must also exist\n", stderr);
    fputs("                   1...16384 values\n", stderr);
    fputs("  CLI code generated: 20Aug26\n", stderr);
    fputs("  ", stderr);
    exit(EXIT_FAILURE);
}

/*********************************************************************/

Cmdline *parseCmdline(int argc, char **argv)
{
    int i;

    Program = argv[0];
    cmd.full_cmd_line = catArgv(argc, argv);
    for (i = 1, cmd.argc = 1; i < argc; i++) {

        if (0 == strcmp("--", argv[i])) {
            while (++i < argc)
                argv[cmd.argc++] = argv[i];
            continue;
        }

        if (0 == strcmp("-ncpus", argv[i])) {
            int keep = i;
            cmd.ncpusP = 1;
            i = getIntOpt(argc, argv, i, &cmd.ncpus, 1);
            cmd.ncpusC = i - keep;
            checkIntHigher("-ncpus", &cmd.ncpus, cmd.ncpusC, 1);
            continue;
        }

        if (0 == strcmp("-lobin", argv[i])) {
            int keep = i;
            cmd.lobinP = 1;
            i = getIntOpt(argc, argv, i, &cmd.lobin, 1);
            cmd.lobinC = i - keep;
            checkIntHigher("-lobin", &cmd.lobin, cmd.lobinC, 0);
            continue;
        }

        if (0 == strcmp("-numharm", argv[i])) {
            int keep = i;
            cmd.numharmP = 1;
            i = getIntOpt(argc, argv, i, &cmd.numharm, 1);
            cmd.numharmC = i - keep;
            checkIntLower("-numharm", &cmd.numharm, cmd.numharmC, 32);
            checkIntHigher("-numharm", &cmd.numharm, cmd.numharmC, 1);
            continue;
        }

        if (0 == strcmp("-zmax", argv[i])) {
            int keep = i;
            cmd.zmaxP = 1;
            i = getIntOpt(argc, argv, i, &cmd.zmax, 1);
            cmd.zmaxC = i - keep;
            checkIntLower("-zmax", &cmd.zmax, cmd.zmaxC, 1200);
            checkIntHigher("-zmax", &cmd.zmax, cmd.zmaxC, 0);
            continue;
        }

        if (0 == strcmp("-wmax", argv[i])) {
            int keep = i;
            cmd.wmaxP = 1;
            i = getIntOpt(argc, argv, i, &cmd.wmax, 1);
            cmd.wmaxC = i - keep;
            checkIntLower("-wmax", &cmd.wmax, cmd.wmaxC, 4000);
            checkIntHigher("-wmax", &cmd.wmax, cmd.wmaxC, 0);
            continue;
        }

        if (0 == strcmp("-sigma", argv[i])) {
            int keep = i;
            cmd.sigmaP = 1;
            i = getFloatOpt(argc, argv, i, &cmd.sigma, 1);
            cmd.sigmaC = i - keep;
            checkFloatLower("-sigma", &cmd.sigma, cmd.sigmaC, 30.0);
            checkFloatHigher("-sigma", &cmd.sigma, cmd.sigmaC, 1.0);
            continue;
        }

        if (0 == strcmp("-rlo", argv[i])) {
            int keep = i;
            cmd.rloP = 1;
            i = getDoubleOpt(argc, argv, i, &cmd.rlo, 1);
            cmd.rloC = i - keep;
            checkDoubleHigher("-rlo", &cmd.rlo, cmd.rloC, 0.0);
            continue;
        }

        if (0 == strcmp("-rhi", argv[i])) {
            int keep = i;
            cmd.rhiP = 1;
            i = getDoubleOpt(argc, argv, i, &cmd.rhi, 1);
            cmd.rhiC = i - keep;
            checkDoubleHigher("-rhi", &cmd.rhi, cmd.rhiC, 0.0);
            continue;
        }

        if (0 == strcmp("-flo", argv[i])) {
            int keep = i;
            cmd.floP = 1;
            i = getDoubleOpt(argc, argv, i, &cmd.flo, 1);
            cmd.floC = i - keep;
            checkDoubleHigher("-flo", &cmd.flo, cmd.floC, 0.0);
            continue;
        }

        if (0 == strcmp("-fhi", argv[i])) {
            int keep = i;
            cmd.fhiP = 1;
            i = getDoubleOpt(argc, argv, i, &cmd.fhi, 1);
            cmd.fhiC = i - keep;
            checkDoubleHigher("-fhi", &cmd.fhi, cmd.fhiC, 0.0);
            continue;
        }

        if (0 == strcmp("-inmem", argv[i])) {
            cmd.inmemP = 1;
            continue;
        }

        if (0 == strcmp("-photon", argv[i])) {
            cmd.photonP = 1;
            continue;
        }

        if (0 == strcmp("-median", argv[i])) {
            cmd.medianP = 1;
            continue;
        }

        if (0 == strcmp("-locpow", argv[i])) {
            cmd.locpowP = 1;
            continue;
        }

        if (0 == strcmp("-zaplist", argv[i])) {
            int keep = i;
            cmd.zaplistP = 1;
            i = getStringOpt(argc, argv, i, &cmd.zaplist, 1);
            cmd.zaplistC = i - keep;
            continue;
        }

        if (0 == strcmp("-baryv", argv[i])) {
            int keep = i;
            cmd.baryvP = 1;
            i = getDoubleOpt(argc, argv, i, &cmd.baryv, 1);
            cmd.baryvC = i - keep;
            checkDoubleLower("-baryv", &cmd.baryv, cmd.baryvC, 0.1);
            checkDoubleHigher("-baryv", &cmd.baryv, cmd.baryvC, -0.1);
            continue;
        }

        if (0 == strcmp("-otheropt", argv[i])) {
            cmd.otheroptP = 1;
            continue;
        }

        if (0 == strcmp("-noharmpolish", argv[i])) {
            cmd.noharmpolishP = 1;
            continue;
        }

        if (0 == strcmp("-noharmremove", argv[i])) {
            cmd.noharmremoveP = 1;
            continue;
        }

        if (argv[i][0] == '-') {
            fprintf(stderr, "\n%s: unknown option `%s'\n\n",
                    Program, argv[i]);
            usage();
        }
        argv[cmd.argc++] = argv[i];
    }

    cmd.argv = argv + 1;
    cmd.argc -= 1;

    if (1 > cmd.argc) {
        fprintf(stderr,
                "%s: there should be at least 1 non-option argument(s)\n",
                Program);
        exit(EXIT_FAILURE);
    }
    if (16384 < cmd.argc) {
        fprintf(stderr,
                "%s: there should be at most 16384 non-option argument(s)\n",
                Program);
        exit(EXIT_FAILURE);
    }
    return &cmd;
}
