.\" pyCLIG manual page
.\" Based on clig by Harald Kirsch (C) 1995-2004 (clig@geggus.net)
.\" pyCLIG (C) 2026 Scott Ransom and Claude (Anthropic)
.\"
.\" This file was generated by pyclig -- command line interface generator
.\"
.\" cligPart markers delimit auto-generated sections.
.\" Remove a pair to hand-edit that section instead.
.\"
.\" cligPart TITLE
.TH "accelsearch" 1 "20Aug26" "pyCLIG-manuals" "Programmer's Manual"
.\" cligPart TITLE end

.\" cligPart NAME
.SH NAME
accelsearch \- Search an FFT or short time series for pulsars using a Fourier domain acceleration search with harmonic summing.
.\" cligPart NAME end

.\" cligPart SYNOPSIS
.SH SYNOPSIS
.B accelsearch
[-ncpus ncpus]
[-lobin lobin]
[-numharm numharm]
[-zmax zmax]
[-wmax wmax]
[-sigma sigma]
[-rlo rlo]
[-rhi rhi]
[-flo flo]
[-fhi fhi]
[-inmem]
[-photon]
[-median]
[-locpow]
[-zaplist zaplist]
[-baryv baryv]
[-otheropt]
[-noharmpolish]
[-noharmremove]
[--] infile ...
.\" cligPart SYNOPSIS end

.\" cligPart OPTIONS
.SH OPTIONS
.IP -ncpus
Number of processors to use with OpenMP,
.br
1 Int value between 1 and oo.
.br
Default: `1'
.IP -lobin
The first Fourier frequency in the data file,
.br
1 Int value between 0 and oo.
.br
Default: `0'
.IP -numharm
The number of harmonics to sum (power-of-two),
.br
1 Int value between 1 and 32.
.br
Default: `8'
.IP -zmax
The max (+ and -) Fourier freq deriv to search,
.br
1 Int value between 0 and 1200.
.br
Default: `200'
.IP -wmax
The max (+ and -) Fourier freq double derivs to search,
.br
1 Int value between 0 and 4000.
.IP -sigma
Cutoff sigma for choosing candidates,
.br
1 Float value between 1.0 and 30.0.
.br
Default: `2.0'
.IP -rlo
The lowest Fourier frequency (of the highest harmonic!) to search,
.br
1 Double value between 0.0 and oo.
.IP -rhi
The highest Fourier frequency (of the highest harmonic!) to search,
.br
1 Double value between 0.0 and oo.
.IP -flo
The lowest frequency (Hz) (of the highest harmonic!) to search,
.br
1 Double value between 0.0 and oo.
.br
Default: `1.0'
.IP -fhi
The highest frequency (Hz) (of the highest harmonic!) to search,
.br
1 Double value between 0.0 and oo.
.br
Default: `10000.0'
.IP -inmem
Compute full f-fdot plane in memory.  Very fast, but only for short time series..
.IP -photon
Data is poissonian so use freq 0 as power normalization.
.IP -median
Use block-median power normalization (default).
.IP -locpow
Use double-tophat local-power normalization (not usually recommended).
.IP -zaplist
A file of freqs+widths to zap from the FFT (only if the input file is a *.[s]dat file),
.br
1 String value
.IP -baryv
The radial velocity component (v/c) towards the target during the obs,
.br
1 Double value between -0.1 and 0.1.
.br
Default: `0.0'
.IP -otheropt
Use the alternative optimization (for testing/debugging).
.IP -noharmpolish
Do not use 'harmpolish' by default.
.IP -noharmremove
Do not remove harmonically related candidates (never removed for numharm = 1).
.IP infile
Input file name(s) of the floating point .fft or .[s]dat file(s).  '.inf' file(s) of the same name must also exist.
.\" cligPart OPTIONS end

.\" cligPart DESCRIPTION
.SH DESCRIPTION
accelsearch searches a Fourier power spectrum
.RB ( .fft ,
normally produced by
.BR realfft (1))
or a short time series
.RB ( .dat / .sdat )
for pulsar signals, using a Fourier-domain acceleration search with
harmonic summing.  At each candidate frequency it searches a range of
constant Fourier-frequency derivatives
.RB ( -zmax ,
approximating a constant acceleration during the observation) and,
optionally, constant second derivatives
.RB ( -wmax ,
approximating a constant jerk), summing
.B -numharm
harmonics (a power of two, up to 32) at each trial to improve
sensitivity to narrow pulsar signals.  Larger
.B -zmax
and
.B -wmax
searches find more strongly accelerated or jerking binaries (e.g.
pulsars with massive companions) at the cost of a much larger search
volume and run time; the PRESTO FAQ has guidance on reasonable ranges
for various companion masses and orbital periods.
.PP
The Fourier-domain acceleration search technique implemented here is
described in Ransom, Eikenberry, & Middleditch (2002),
https://ui.adsabs.harvard.edu/abs/2002AJ....124.1788R/abstract, and
the jerk-search extension
.RB ( -wmax )
is described in Andersen & Ransom (2018),
https://ui.adsabs.harvard.edu/abs/2018ApJ...863L..13A/abstract.  If
accelsearch contributes to a publication, please cite the relevant
paper(s) (see also CITATION.cff in the PRESTO repository).
.PP
Candidates above
.B -sigma
(the equivalent Gaussian significance, corrected for the number of
independent frequencies and derivatives searched in this run) are
written to a
.I .cand
text summary and a binary
.I _ACCEL_<zmax>
(or
.I _ACCEL_<zmax>_JERK_<wmax>
when
.B -wmax
is used) candidate file for later sifting (e.g. with
.IR ACCEL_sift.py )
and folding with
.BR prepfold (1).
.B -inmem
computes the full f/fdot plane in memory instead of incrementally,
which is much faster but only practical for short time series.  When
searching a
.I .dat/.sdat
time series directly (rather than a pre-computed
.IR .fft ),
.B -zaplist
removes known interference frequencies from the internally-computed
FFT before the search runs, without a separate
.BR zapbirds (1)
pass; in that case (and only in that case)
.B -baryv
should be given if the time series was barycentered, so that the
zapped birdie frequencies (known in the topocentric frame) are
shifted correctly before zapping.  The zapped bins are replaced the
same way
.BR zapbirds (1)
replaces them, with noise drawn to match the local power level.
.PP
.B -ncpus
sets the number of OpenMP threads used to parallelize the search.
accelsearch is one of the few PRESTO routines where multiple CPUs
give a substantial speedup (the others being
.BR rfifind (1)
and
.BR prepfold_multi (1));
most of the rest of PRESTO is effectively single-threaded, with
parallelism instead coming from running independent searches (e.g.
different DM trials) concurrently.
.PP
Example: a zmax=200 acceleration search of a barycentered time series'
FFT, summing up to 8 harmonics:
.RS
.nf
realfft mypulsar_DM10.00.dat
accelsearch -zmax 200 -numharm 8 mypulsar_DM10.00.fft
.fi
.RE

.\" cligPart DESCRIPTION end

.\" cligPart SEE ALSO
.SH SEE ALSO
prepdata(1), prepsubband(1), realfft(1), zapbirds(1), prepfold(1), rfifind(1), prepfold_multi(1)
.\" cligPart SEE ALSO end

.\" cligPart AUTHOR
.SH AUTHOR
Scott Ransom <sransom@nrao.edu>
.\" cligPart AUTHOR end
